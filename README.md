# Hotel Recommendation Dashboard
Multimodal CLIP + OCR system.

## Run locally
pip install -r requirements.txt
streamlit run extract_1.py

## Dataset
50 hotel ads in vector_store.pkl (generate with generate_dataset.
